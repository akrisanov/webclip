# webclip

Extensible CLI for saving web pages, comments, and assets into Obsidian-friendly archives.

## Quick start

```bash
uv venv
uv sync
uv run webclip --help
```

## Common commands

Using Make:

```bash
make check
```

Using just:

```bash
just check
```
